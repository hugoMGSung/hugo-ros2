# ROS 2 Programming 기초

## 로봇 플랫폼 

### 정의

- 로봇 플랫폼에 SW, HW 존재함
- 로봇 구현방법
    - 주변인식, 자기위치 추정, 지도, 하드웨어 제어 등 다양한 기능이 필요함

- 현재의 로봇 개발
    - 로봇 인터페이스, 하드웨어 추상화, 메시지 통신 표준 정의 등 진행

- ROS의 선택이유
    - 라이브러리 풍부
    - 개발 편의성
    - 확장성
    - 다양한 커뮤니티 
    - etc


### ROS

- Robot Operating System 
    - 로봇 소프트웨어 개발을 쉽게 하도록 운영체제와 비슷한 역할을 하는 도구(Middleware). OS는 아님.
    - 보통 Linux(중 Ubuntu) 상에 설치하여 사용함

#### 역사

- 미국 스탠포드 대학 박사과정 학생들에 의해 `Switchyard` 라는 이름의 프로젝트에서 시작
- 2007년 PR2라는 로봇 개발. 여기에서 실행되는 SW로 ROS라는 이름 사용 시작
- 2010년 ROS 1.0이 배포 시작


#### 주요 개념

- Node : ROS에서 최소 단위 실행 프로세스. 최소한 실행가능한 프로그램과 동일
- Package : 하나 이상의 노드나 노드 실행을 위한 정보를 묶어 놓은 것.
    - 메타 패키지 : 패키지의 묶음
- Message : 노드간의 데이터를 주고 받는 형태, 인터페이스라고 부름. 프로그래밍의 변수 형태(Integer, Float, Boolean 등)
    - Topic, Service, Action으로 구분

- Topic : 노드 간 정보 전달 채널. ROS의 메시지 중 80~90%의 통신방식
    - 비동기 단방향 메시지 송수신 방식, 센서값 

- Service : 동기식 양방향 메시지 송수신 방식
    - SRV 인터페이스 형태의 서비스를 요청하는 Service Client, 응답하는 Service Server로 구성

- Action : 피드백이 필요한 장시간 실행 작업, 예를 들어 로봇을 특정 위치로 이동시키거나 복잡한 동작을 수행하도록 요청하는 작업에 사용
    - Goal을 지정하는 Action Client, 액션 목표를 받아 특정 태스크를 수행하면서 중간 결과값을 전송하는 액션 피드백(Feedback), 최종 결과값을 담은 액션 결과(Result)를 전송하는 Action Server 간의 통신


#### ROS1과 ROS2 차이
- ROS 프로그래밍에 있어서 ROS1과 ROS2의 공통된 중요한 핵심 개념
- 보안 강화
- 멀티플랫폼 지원 (Linux, macOS, Windows)
- 실시간 제어 지원
- 최신 기술 표준 지원
- 불안정한 네트워크에 대한 대응
- 상업용 제품 개발 가능
- ROS1과의 호환성 확보

